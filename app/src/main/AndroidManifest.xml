<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.babycloset">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".UI.Activity.Signin2Activity"/>
        <activity android:name=".UI.Activity.SigninActivity"/>
        <activity android:name=".UI.Activity.ShareProductActivity"/>
        <activity android:name=".UI.Activity.EditInfoActivity"/>
        <activity android:name=".UI.Activity.RatingActivity"/>
        <activity android:name=".UI.Activity.ListPeopleActivity"/>
        <activity android:name=".UI.Adapter.BlankActivity"/>

        <activity android:name=".UI.Activity.InfoConsentActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>